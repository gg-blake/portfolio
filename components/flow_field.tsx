import { useEffect } from "react";

export default function FlowFieldBG() {
    useEffect(() => {
        
    }, [])

    return (
        <canvas id="flow-field" className="absolute top-0 left-0 w-screen h-screen bg-black stroke-white"></canvas>
    )
}